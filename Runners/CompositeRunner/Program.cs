//using Runners.Shared;
//using Runners.Shared.Runners;

class Runner
{
    //    public static async Task<int> Main()
    //    {
    //        using var cts = new CancellationTokenSource();
    //        Console.CancelKeyPress += (s, e) =>
    //        {
    //            e.Cancel = true;
    //            cts.Cancel();
    //        };

    //        var supportedRunners = new Dictionary<string, IRunner>
    //        {
    //            {"csharp", new DotNetRunner() },
    //            {"java", new JavaCompositeRunner() },
    //            {"kotlin",new KotlinCompositeRunner() },
    //            {"nodejs", new NodeJsRunner() },
    //            {"typescript", new TypeScriptRunner() },
    //            {"mssql", new MsSqlRunner() },
    //            {"postgresql",new PostgresqlRunner() }
    //        };

    //        using (var runner = new CompositeRunner(supportedRunners))
    //        {
    //            await runner.StartAsync(cts.Token);

    //            await runner.ListenAsync(cts.Token);

    //            await runner.StopAsync(cts.Token);
    //        }

    //        return 0;
    //    }

    static void Main()
    {

    }

}
